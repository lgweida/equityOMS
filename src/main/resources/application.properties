spring.application.name=equityOMS

server.port=8081

spring.datasource.url=jdbc:h2:mem:fixgatewaydb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# WebSocket
spring.websocket.sockjs.path=/fixgateway-websocket

app.fix.clordid.prefix=ALGO
app.fix.clordid.trader=01

# PostgreSQL DataSource Configuration
# # spring.datasource.url=jdbc:postgresql://localhost:5432/equity_oms
# # spring.datasource.username=postgres
# # spring.datasource.password=password
# # spring.datasource.driver-class-name=org.postgresql.Driver

# # # Hibernate/JPA Configuration
# # spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# # spring.jpa.hibernate.ddl-auto=none
# # spring.jpa.show-sql=true
# # spring.jpa.properties.hibernate.format_sql=true

# # Liquibase Configuration
# spring.liquibase.change-log=classpath:/db/changelog/changelog_001.yml
# spring.liquibase.enabled=true
# spring.liquibase.default-schema=public